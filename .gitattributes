class MyIterable:
    def __init__(self, limit):
        self.limit = limit

    def __iter__(self):
        current = 0
        while current < self.limit:
            yield current
            current += 1
